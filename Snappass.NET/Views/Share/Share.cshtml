@{
    ViewData["Title"] = "ViewPassword";
    Layout = "~/Views/_Layout.cshtml";
}


<div class="flex min-h-full md:items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
    <div class="w-full max-w-md space-y-8">
        <div>
            <img class="mx-auto h-12 w-auto" src="~/logoipsum-262.svg" alt="Your Company">
            @if (ViewData["Expired"] != null)
            {
                <div class="bg-red-100 rounded-lg py-5 px-6 mt-4 text-base text-red-700 mb-3" role="alert">
                    Link expired. Try sharing another secret!
                </div>
            }
            <h2 class="mt-6 text-center text-3xl font-bold tracking-tight text-gray-900">Share secret</h2>
            <p class="mt-2 text-center text-sm text-gray-600">
                Stop sharing sensitive data over email and messaging platforms.
            </p>
        </div>
        <form class="mt-8 space-y-6" role="form" id="password_create" method="post" autocomplete="off">
            <div class="rounded-md shadow-sm">
                <div>
                    <label for="password" class="sr-only">Password</label>
<textarea id="password" name="password" autofocus="true" rows="5" class="relative block w-full appearance-none rounded-none rounded-t-md border border-gray-300 px-3 py-2 text-gray-900 placeholder-gray-500 focus:z-10 focus:border-indigo-500 focus:outline-none focus:ring-indigo-500 sm:text-sm" placeholder="Enter sensitive data to securely share..." aria-describedby="basic-addon1" autocomplete="off" required></textarea>
                </div>
                <div>
                    <button id="generate-pw" type="button" class="relative block w-full appearance-none rounded-none rounded-b-md border border-transparent bg-slate-400 py-1 px-4 text-sm font-medium text-white hover:bg-slate-500 focus:border-slate-500 focus:outline-none focus:ring-slate-500">Generate Password</button>
                </div>
            </div>
            <div class="flex items-center justify-between">
                <label for="ttl" class="ml-2 block w-1/3 text-sm text-gray-900">Expire after one</label>
                <select id="ttl" name="ttl" autocomplete="country-name" class="mt-1 block w-2/3 rounded-md border border-gray-300 bg-white py-2 px-3 shadow-sm focus:border-indigo-500 focus:outline-none focus:ring-indigo-500 sm:text-sm">
                    <option value="Hour">Hour</option>
                    <option value="Day" selected>Day</option>
                    <option value="Week">Week</option>
                    <option value="Month">Month</option>
                </select>
            </div>
            <div>
                <button type="submit" class="group relative flex w-full justify-center rounded-md border border-transparent bg-indigo-600 py-2 px-4 text-sm font-medium text-white hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2">
                    <span class="absolute inset-y-0 left-0 flex items-center pl-3">
                        <!-- Heroicon name: mini/lock-closed -->
                        <svg class="h-5 w-5 text-indigo-500 group-hover:text-indigo-400" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                            <path fill-rule="evenodd" d="M10 1a4.5 4.5 0 00-4.5 4.5V9H5a2 2 0 00-2 2v6a2 2 0 002 2h10a2 2 0 002-2v-6a2 2 0 00-2-2h-.5V5.5A4.5 4.5 0 0010 1zm3 8V5.5a3 3 0 10-6 0V9h6z" clip-rule="evenodd" />
                        </svg>
                    </span>
                    Share secret
                </button>
            </div>
        </form>
    </div>
</div>

<script src="~/js/generate_pw.js"></script>